import React from 'react';
import { IPage, ISection } from '../../ts/interfaces';

const Section = (props: ISection): JSX.Element => {
  return <section className="section"></section>;
};

export default Section;
